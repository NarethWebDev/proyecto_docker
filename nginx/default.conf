server {
    listen 80;
    server_name localhost;

    location / {
        proxy_pass http://backend_sofia:5050;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarder-For $proxy_add_x_fowarded_for;
    }
}